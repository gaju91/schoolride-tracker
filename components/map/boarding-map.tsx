"use client";
import { MapContainer,Marker,Popup,TileLayer } from "react-leaflet";import L from "leaflet";import type { BoardingEvent,Student } from "@/domain/boarding/boarding.types";
const icon=L.divIcon({className:"",html:'<div style="background:#0f766e;color:white;border:3px solid white;width:28px;height:28px;border-radius:50%;display:grid;place-items:center;box-shadow:0 2px 8px #1238">●</div>',iconSize:[28,28]});
export function BoardingMap({events,students}:{events:BoardingEvent[];students:Student[]}){const markers=events.filter(e=>e.location.latitude!=null&&e.location.longitude!=null);return <MapContainer center={[26.9124,75.7873]} zoom={13} scrollWheelZoom={false}><TileLayer attribution='&copy; OpenStreetMap contributors' url={process.env.NEXT_PUBLIC_MAP_TILE_URL||"https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"}/>{markers.map(e=>{const student=students.find(s=>s.id===e.studentId);return <Marker key={e.id} position={[e.location.latitude!,e.location.longitude!]} icon={icon}><Popup><b>{student?.name}</b><br/>{new Date(e.occurredAt).toLocaleTimeString("en-IN",{hour:"numeric",minute:"2-digit",timeZone:"Asia/Kolkata"})}<br/>{e.location.status==="simulated"?"Simulated location":"Device location"}</Popup></Marker>})}</MapContainer>}
