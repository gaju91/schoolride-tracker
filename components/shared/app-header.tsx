"use client";
import Link from "next/link";import { useRouter } from "next/navigation";import { clearRole } from "@/lib/session";
export function AppHeader({title,subtitle}:{title:string;subtitle?:string}){const router=useRouter();return <header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-4"><Link href="/" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-teal text-xl text-white">↗</span><span><b>SchoolRide</b><span className="block text-xs text-slate-500">Tracker</span></span></Link><div className="min-w-0 flex-1"><h1 className="truncate text-lg font-bold">{title}</h1>{subtitle&&<p className="truncate text-xs text-slate-500">{subtitle}</p>}</div><button className="btn btn-secondary text-sm" onClick={()=>{clearRole();router.push("/")}}>Sign out</button></div></header>}
