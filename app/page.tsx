"use client";
import { useRouter } from "next/navigation";import type { Role } from "@/domain/boarding/boarding.types";import { resetDemoState } from "@/lib/demo-store";import { setRole } from "@/lib/session";
const roles:[Role,string,string,string][]=[["super_admin","Super Admin","Platform overview","/super-admin"],["school_admin","School Admin","Live transport operations","/admin"],["driver","Driver","Scan and confirm boarding","/driver"],["parent","Parent","Child status and alerts","/parent"]];
export default function Login(){const router=useRouter();return <main className="min-h-[calc(100vh-28px)] bg-[radial-gradient(circle_at_top_left,#dff4ed,transparent_45%)] px-4 py-10"><div className="mx-auto max-w-5xl"><div className="mb-10 max-w-2xl"><div className="mb-5 grid h-16 w-16 place-items-center rounded-2xl bg-teal text-3xl text-white">↗</div><p className="label">Discovery proof of concept</p><h1 className="mt-2 text-4xl font-black tracking-tight text-navy sm:text-5xl">Every boarding, visible.</h1><p className="mt-4 text-lg text-slate-600">A calm, real-time connection between the school bus, transport desk, and family.</p></div><section aria-label="Demo roles" className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{roles.map(([role,title,desc,path])=><button key={role} onClick={()=>{setRole(role);router.push(path)}} className="card min-h-44 p-5 text-left hover:-translate-y-1 hover:border-teal"><span className="label">Enter as</span><h2 className="mt-4 text-xl font-bold">{title}</h2><p className="mt-2 text-sm text-slate-500">{desc}</p><span className="mt-5 inline-block font-bold text-teal">Open workspace →</span></button>)}</section><div className="mt-8 flex items-center justify-between gap-4 text-sm"><button className="btn btn-secondary" onClick={()=>{if(confirm("Restore the fictional demo data?"))resetDemoState()}}>Reset demo data</button><span className="text-slate-500">Build 0.1.0 · All people and schools are fictional</span></div></div></main>}
