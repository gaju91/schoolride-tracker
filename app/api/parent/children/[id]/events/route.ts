import { failure,success } from "@/lib/api";import { getServerState,students,IDS } from "@/lib/server-demo-store";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const student=students.find(s=>s.id===id&&s.parentUserId===IDS.parent);if(!student)return failure("FORBIDDEN","This child is not assigned to the demo parent.",403);return success({student,events:getServerState().events.filter(e=>e.schoolId===student.schoolId&&e.studentId===id),notifications:getServerState().notifications.filter(n=>n.studentId===id&&n.parentUserId===IDS.parent)})}
